# scripts

| File | Purpose |
| --- | --- |
| (future) `release-notes.ts` | Changelog post-processing for releases (v0.5+) |
| (future) `codemods/` | Per-release codemods for breaking changes (v1.0+) |

CEM generation runs in each package via `pnpm cem` (configured at `packages/*/custom-elements-manifest.config.mjs`).
