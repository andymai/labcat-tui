# @labcat/tui-themes

Additional theme presets for `@labcat/tui` — gruvbox, rose-pine, kanagawa, synthwave, phosphor (green/amber), plus light variants. **Empty in v0.1**; themes land in v0.5+. The `claude` / `claudeLight` themes ship in `@labcat/tui` core.
